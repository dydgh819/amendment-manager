# 개정 예정 법령 검토 프로그램 — 단계별 Claude Code 구현 프롬프트

PRD(개정예정법령검토프로그램_PRD.md) 기준. 순서대로 하나씩 Claude Code에 투입.
각 단계는 이전 단계 결과물 위에서 진행되며, 완료 후 동작 확인 후 다음 단계로 넘어갈 것.

---

## STEP 1. 프로젝트 뼈대 + Firebase Cloud Function 프록시 기본형

```
Firebase 프로젝트에 Cloud Function을 하나 생성해줘.
목적: open.law.go.kr Open API 호출을 서버 측에서 대신 수행하는 프록시.

요구사항:
- 함수명: fetchLawApi (HTTP 트리거)
- 파라미터: target(예: eflaw), 법령ID 또는 MST, 기타 쿼리 파라미터를 그대로 전달받아
  http://www.law.go.kr/DRF/lawSearch.do 또는 lawService.do 로 전달
- OC 키는 환경변수(functions config 또는 .env)로 관리, 프론트에는 절대 노출하지 않음
- 응답은 JSON으로 변환해서 반환 (Open API가 XML을 주면 파싱해서 JSON으로 통일)
- CORS 허용 설정 포함
- 에러 발생 시 상태코드와 에러메시지를 JSON으로 반환하는 표준 에러 핸들링 포함
```

---

## STEP 2. 시행일 법령 목록 조회 + 시행대기 건 탐지 로직

```
STEP 1의 fetchLawApi 프록시를 사용해서, PRD 부록에 있는 15개 법령(법령ID 목록)에 대해
target=eflaw 로 시행일 법령 목록을 조회하는 스크립트/함수를 만들어줘.

요구사항:
- 15개 법령ID를 순회하며 각각 eflaw 조회
- 응답에서 시행일자(efYd)가 오늘 이후인 건만 필터링 (= 공포됐지만 아직 시행 전)
- 결과를 { 법령ID, 법령명, 공포번호, 공포일, 시행예정일 } 형태로 정리해서 콘솔에 출력
- 이번 단계에서는 아직 Firestore 저장 없이 로직 검증까지만 (다음 단계에서 저장 붙임)
- PRD 부록의 "현재 감지된 시행예정 개정 건 5건"과 결과가 일치하는지로 정확성 검증
```

---

## STEP 3. Firestore 저장 + 신규 건 중복 방지 로직

```
STEP 2의 탐지 결과를 Firestore에 저장하는 로직을 추가해줘.

요구사항:
- 컬렉션: pendingAmendments
- 문서 ID: 법령ID + 공포번호 조합 (동일 개정 건 중복 저장 방지)
- 저장 필드: 법령ID, 법령명, 공포번호, 공포일, 시행예정일, 감지일시, 처리상태(pending/summarized)
- 이미 저장된 문서는 스킵, 새로 감지된 건만 insert
- "새로 감지된 건" 목록을 함수 반환값으로도 넘겨줘 (다음 단계에서 바로 요약 처리하도록)
```

---

## STEP 4. 조문별 변경 이력 조회 — 변경 조문 번호 추출

```
STEP 1의 프록시를 확장해서, 국가법령정보 Open API의 "조문별 변경 이력 목록 조회"
(또는 "일자별 조문 개정 이력 목록 조회") API를 호출하는 기능을 추가해줘.

요구사항:
- 입력: 법령ID(또는 MST) + 해당 개정의 공포일자
- 출력: 이번 개정으로 실제 변경된 조문 번호 리스트 (예: ["제38조", "제110조"])
- STEP 3에서 새로 감지된 pendingAmendments 문서마다 이 API를 호출해서
  변경 조문 리스트를 문서에 추가 필드(changedArticles)로 업데이트
- 이 API의 정확한 응답 필드명은 실제 호출해보고 확정 (PRD 리스크 항목 참고),
  호출 결과 원본 응답을 한번 콘솔에 출력해서 구조 확인부터 시작
```

---

## STEP 5. 변경 조문 시행 전/후 본문 조회

```
STEP 4에서 얻은 changedArticles 각각에 대해, 시행 전(현행) 본문과 시행 후(개정 예정) 본문을
각각 조회하는 로직을 만들어줘.

요구사항:
- 현행본: get_law_text 방식대로 MST + jo(조문번호) 파라미터로 조회 (efYd 없이)
- 시행예정본: 동일하게 MST + jo + efYd(시행예정일) 파라미터로 조회
- 조문별로 { 조문번호, 조문제목, 현행본문, 개정본문 } 구조로 정리
- pendingAmendments 문서에 articleDiffs 필드로 저장
```

---

## STEP 6. Claude API 실무 영향 요약 생성

```
STEP 5의 articleDiffs를 Claude API에 넘겨서 실무 영향을 요약하는 함수를 만들어줘.

요구사항:
- 입력: 조문번호, 현행본문, 개정본문, 대상 사업장 맥락(울산 1·2·3공장, 석유화학 공정 안전보건 실무)
- 프롬프트: "이 조문 개정이 실무에 어떤 영향을 주는지 3~5문장으로 요약, 특히 담당자가
  시행 전 준비해야 할 액션이 있으면 명시"
- 출력을 pendingAmendments 문서의 summary 필드에 저장, 처리상태를 'summarized'로 업데이트
- 조문이 여러 개인 경우 조문별로 요약을 배열로 저장
```

---

## STEP 7. 스케줄 함수로 전체 파이프라인 자동화

```
STEP 2~6의 전체 흐름(탐지 → 저장 → 변경조문 추출 → 본문조회 → 요약)을
Firebase Scheduled Function으로 묶어줘.

요구사항:
- 실행 주기: 매일 1회 (cron 표현식으로 새벽 시간대 설정, 추후 조정 가능하게 주석)
- 실행 순서: STEP2 탐지 → STEP3 저장(신규만) → 신규 건에 대해 STEP4~6 순차 실행
- 각 단계 실패 시에도 다음 법령/건 처리는 계속되도록 개별 try-catch 처리
- 전체 실행 로그(성공/실패 건수)를 별도 컬렉션(batchLogs)에 기록
```

---

## STEP 8. 단일 HTML 대시보드 — 알림 카드 UI

```
pendingAmendments 컬렉션을 조회해서 알림 카드 형태로 보여주는 단일 HTML 페이지를 만들어줘.
(기존 인허가검토프로그램과 동일한 패턴: 단일 HTML + 바닐라 JS + Firebase Firestore, 빌드 도구 없음)

요구사항:
- 카드 항목: 법령명 / 조문번호·제목 / 공포일·시행예정일 / Claude 요약 / 현행-개정 본문 비교(토글로 펼쳐보기)
- 처리상태(pending/summarized)별 필터
- Hanwha 브랜드 디자인 (primary orange #E87722) 적용, 기존 인허가검토프로그램과 톤 일치
- 시행일이 임박한 순으로 정렬
```

---

## STEP 9. 기존 인허가검토프로그램과 통합

```
STEP 8에서 만든 대시보드를 기존 인허가검토프로그램 단일 HTML에 탭 또는 배지 형태로 통합해줘.

요구사항:
- 기존 프로그램 화면 상단 등에 "시행예정 개정 N건" 배지 표시
- 배지 클릭 시 STEP 8 대시보드 화면으로 전환 (같은 파일 내 탭 전환 방식 권장, 기존 구조 참고해서 결정)
- 기존 코드 스타일/네이밍 컨벤션 유지
```

---

## STEP 10. 에러 처리 마감 + 테스트 케이스 검증

```
전체 파이프라인에 대해 아래를 점검하고 보완해줘.

- Open API 호출 실패 시 재시도 로직 (최대 N회, 지수 백오프)
- 배치 실행 실패 시 관리자 알림 표시 방법 마련
- PRD 부록의 "현재 감지된 시행예정 개정 5건"(산업안전보건법 3건, 액화석유가스법 1건,
  도시가스사업법 1건)을 기준으로 전체 파이프라인을 한 번 실행해서
  실제로 5건 모두 대시보드에 정상적으로 노출되는지 확인
```
