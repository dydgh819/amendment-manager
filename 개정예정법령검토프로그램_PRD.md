# 최근 개정 예정 법령 검토 프로그램 — PRD

## 1. 개요
- **목적**: 이미 공포되었으나 아직 시행 전인 법령 개정 건을 자동으로 감지하고, 변경된 조문만 추출·요약하여 인허가/안전보건 실무 담당자가 시행 전 미리 대응할 수 있도록 지원
- **연계 프로젝트**: 인허가검토프로그램(기존)과 동일 대시보드 내 알림/배지 형태로 통합

## 2. 범위
### 포함
- 이미 공포(공포번호 확정) → 시행일 도래 전 개정 건
- 법률 / 시행령 / 시행규칙 3단계 전부

### 제외
- 입법예고 단계의 미확정 개정안 (정부입법지원센터 별도 시스템, 이번 버전에서는 다루지 않음)

## 3. 감시 대상 법령
- 5대 법령 × 3단계(법률/시행령/시행규칙) = 최대 15개 법령
- (인허가검토프로그램과 동일한 5대 법령 목록 재사용)

| # | 법령명 |
|---|---|
| 1 | 산업안전보건법 |
| 2 | 고압가스안전관리법 |
| 3 | 에너지이용합리화법 |
| 4 | 액화석유가스의 안전관리 및 사업법 |
| 5 | 도시가스사업법 |

## 4. 데이터 소스 (국가법령정보 공동활용 Open API, open.law.go.kr)
| 단계 | API | 용도 |
|---|---|---|
| 1 | 시행일 법령 목록 조회 (target=eflaw) | 시행 대기 중인 개정 건 탐지, 공포번호/공포일/시행일 확보 |
| 2 | 일자별 조문 개정 이력 목록 조회 | 해당 공포 건에서 실제로 바뀐 조문 번호만 추출 |
| 3 | 시행일 법령 본문 조회 (jo 파라미터) | 바뀐 조문의 시행 전/후 본문 각각 조회 |

## 5. 핵심 처리 플로우
1. (배치, 일/주 단위) 15개 법령에 대해 eflaw 목록 조회 → 시행일 미도래 건 필터링
2. 신규 감지된 개정 건에 대해 일자별 조문 개정 이력 조회 → 변경 조문 번호 리스트 확보
3. 변경 조문만 시행 전/후 본문 각각 조회
4. Claude API에 "변경 조문 전/후 텍스트 + 대상 사업장(울산 1·2·3공장) 맥락"을 넘겨 실무 영향 요약 생성
5. 결과를 대시보드에 조문 단위 알림/배지로 게시 (예: "산업안전보건법 제110조 개정 — 2026.8.1 시행")

## 6. 산출물 형태
- 대시보드 알림 카드: 법령명 / 조문번호·제목 / 공포일·시행일 / 변경 요약(Claude 생성) / 원문 링크(전/후 비교)
- 기존 건 대비 신규 감지 건만 노출 (중복 알림 방지 — 처리 이력 저장 필요)

## 7. 기술 스택 (기존 패턴 준수 → Blaze 요금제 회피를 위해 일부 변경)
- 프론트: 단일 HTML + 바닐라 JS
- 백엔드/배치: **Cloud Function 없음.** Firebase Spark(무료) 플랜의 Cloud Functions는
  구글 소유 API 외 외부 네트워크(law.go.kr 등) 접근이 막혀 있어, law.go.kr·Gemini 호출과
  Firestore 쓰기를 **GitHub Actions**(매일 cron + 수동 workflow_dispatch)로 이전했다.
  OC 키·Gemini 키·Firestore 서비스 계정 키는 GitHub Secrets에만 보관한다.
- 저장: Firebase Firestore(Spark 무료 등급) — 처리된 개정 건 이력(중복 알림 방지), 조문별 변경 요약 캐시
- 대시보드(STEP 8/9)는 Firebase Hosting(Spark)에 올리고, "새로고침" 버튼은 Firestore를
  다시 읽기만 한다 — 파이프라인 즉시 재실행이 필요하면 GitHub Actions 탭에서 수동 실행한다.
- AI 요약: Gemini API (Google AI Studio 무료 티어 모델, 기본값 gemini-2.0-flash — 최초 계획은 Claude API였으나 비용 문제로 변경)

## 8. 비기능 요구사항
- OC 키·Gemini 키·Firestore 서비스 계정 키는 GitHub Actions Secrets에만 보관, 프론트/저장소
  코드에 노출 금지
- API 호출 실패 시 재시도 로직 (Open API 응답 지연/오류 대비)
- 배치 주기 실패 알림 (예: 조회 실패 시 관리자에게 별도 표시) — 현재는 GitHub Actions 워크플로
  자체가 실패로 표시되는 것과 `batchLogs` 컬렉션 기록으로 대체

## 9. 리스크 / 확인 필요 사항
- 조문별 변경 이력 API의 정확한 응답 필드명·구조는 실제 호출 테스트로 확정 필요
- 시행령/시행규칙까지 포함 시 API 호출 수 증가 → 배치 주기·호출량 조정 필요
- Open API 호출 제한(과도한 트래픽 시 차단 여부) 확인 필요

## 부록. 감시 대상 15개 법령 — 법령ID/MST

| 법령 | 구분 | 법령ID | MST |
|---|---|---|---|
| 산업안전보건법 | 법률 | 001766 | 287805 |
| 산업안전보건법 시행령 | 대통령령 | 003786 | 263363 |
| 산업안전보건법 시행규칙 | 고용노동부령 | 007364 | 263749 |
| 고압가스 안전관리법 | 법률 | 001850 | 283919 |
| 고압가스 안전관리법 시행령 | 대통령령 | 002246 | 286839 |
| 고압가스 안전관리법 시행규칙 | 산업통상부령 | 006285 | 286887 |
| 에너지이용 합리화법 | 법률 | 001867 | 276559 |
| 에너지이용 합리화법 시행령 | 대통령령 | 004172 | 286301 |
| 에너지이용 합리화법 시행규칙 | 기후에너지환경부령 | 007704 | 287433 |
| 액화석유가스의 안전관리 및 사업법 | 법률 | 001849 | 276549 |
| 액화석유가스의 안전관리 및 사업법 시행령 | 대통령령 | 004132 | 278387 |
| 액화석유가스의 안전관리 및 사업법 시행규칙 | 산업통상부령 | 007667 | 282369 |
| 도시가스사업법 | 법률 | 001851 | 276497 |
| 도시가스사업법 시행령 | 대통령령 | 003420 | 284727 |
| 도시가스사업법 시행규칙 | 산업통상부령 | 007095 | 285295 |

※ MST는 법령 개정 시 바뀔 수 있으므로, 배치 로직에서는 법령ID를 기준으로 재검색 후 최신 MST를 매번 확인하는 방식을 권장

## 부록. 현재(2026-07-26 기준) 감지된 시행예정 개정 건 — 테스트 케이스로 활용 가능
| 법령 | 개정 종류 | 공포일 | 시행예정일 |
|---|---|---|---|
| 산업안전보건법 | 일부개정 | 2026-07-07 | 2027-01-08 |
| 산업안전보건법 | 일부개정 | 2026-02-19 | 2026-08-01 / 2027-01-01 |
| 산업안전보건법 | 타법개정 | 2026-04-07 | 2026-12-08 |
| 액화석유가스의 안전관리 및 사업법 | 일부개정 | 2025-12-30 | 2026-12-31 |
| 도시가스사업법 | 일부개정 | 2026-05-26 | 2026-11-27 |

## 10. 향후 확장 (이번 버전 범위 아님)
- 입법예고 단계(정부입법지원센터) 연동 — 진짜 "예정" 단계까지 확장
- 자치법규(울산 지역 조례) 개정 감시 추가
